<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Ketch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/ic_launcher_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Ketch.Main</item>
    </style>

    <!--
      Ketch is dark-only (docs/design_document.md §1), so the window theme is
      pinned dark rather than DayNight: on a light-mode device a DayNight parent
      flashes a white window before Compose draws the first dark frame.
    -->
    <style name="Theme.Ketch.Main" parent="android:Theme.DeviceDefault">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
